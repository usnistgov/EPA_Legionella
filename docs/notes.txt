Tasks to complete.
1)	For the src/rh_temp_other_analysis.py script, add the AIO2 temp and HR to the analysis and figures. For the figure labels, it will be “AIO2 OutsideS”; this instrument will be the last one in the list (i.e., … Aranet4 OutsideN, QuantAQ OutsideS, AIO2 OutsideS.
2)	Change where figures are saved for all scripts. Keep all figures as they are, other than the event_## figures. For the event_## figures, save them in a new subdirectory of output/plots. Call the subdirectory event_figures (i.e., output/plots/event_figures). Update the data_config.json and data_paths.py as needed for this change.
3)	For the rh_data in the src/particle_decay_analysis.py script, change the data being used to be the average of all bedroom instruments (Vaisala Bedroom, HOBO Bedroom1, HOBO Bedroom2, HOBO Bedroom3, Arenet4 Bedroom, and QuantAQ Bedroom) available during a given temp event. Do not load and process this data as part of the src/particle_decay_analysis.py script; instead, have the src/rh_temp_other_analysis.py script complete this task and save the data to a new sheet in the rh_temp_wind_summary.xlsx. For the rh_temp_wind_summary.xlsx sheet, also save the std and uncertainty of the average as well, but don’t use it for the figures. Then just have the src/particle_decay_analysis.py script load that data. Update the data_config.json and data_paths.py as needed for this change.
4)	For the src/particle_decay_analysis.py, remove the --allow-negative-beta argument. Instead, make this the default for the script. Some changes to the logic are needed, though. For the emission rate calculation, nothing needs to change; the inclusion of negative values is not a problem. For the beta values, however, more logical checks are needed. This will need to be a multi-step process that calculates betas, including negative values. If the R² for these calculations is ±0.8, the values are kept and used. For the betas that failed that check, they are recalculated without the negative values. If the new R² for these calculations is ±0.8, the values are kept and used. For the betas that still failed, they are set to 0 as before when they were clamped.
5)	Use the emission_etotal_boxplot_....png plots as a guideline, make similar figures for the penetration factor and for the emission rate.
6)	Since the deposition_rate_boxplot_....png, emission_etotal_boxplot_....png, emission_rate_boxplot_....png, and penetration_factor_boxplot_....png will all have the same configuration, formatting, etc., these configurations should be moved to the central utility in the plot_style. This utility may also be used for the air_change_rate_boxplot.png (or other similar figures), as it is very similar.
7)	Make ten new emission_etotal_boxplot_....png type figures. These ten figures will be divided up by the same bin0-2 and bin3-6 as the existing ones are (the existing figures will stay as they are). These new figures will be exactly like the current ones with one change, instead of the x-axis being water configuration temperature (w48, w11, etc.) the x-axis will be the average bedroom RH (same average RH from part 3 above), average bedroom temperature (calculated with the same method as RH from part 3 above (can be calculated and saved to the same sheet as RH as well)), average air change rate (should already be part of the co2_lambda_overall_summary.csv, verify this), beta_deposition (can be calculated from the particle_analysis_summary.xlsx), or the p_penetration (can be calculated from the particle_analysis_summary.xlsx) for each pair of figures. The data will still be the Etotal data grouped by the water configuration temperature, but its placement along the x-axis will be determined by the new average values. 