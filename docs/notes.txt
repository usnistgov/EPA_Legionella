Write a utility script that handles significant figures for calculated data. Add an argument (--no-sig-figs) to all _analysis.py scripts that will have all three scripts run as they are right now, but without the argument, the scripts will write all calculated data (with the exception of the data written to the rh_temp_wind_summary’s  Bedroom_Conditions sheet as this data is used for farther calculations) in CSV and Excel files, as well as those values displayed on figures to 2 significant figures. Have the number of sig figs set in the utility script. Is this the most appropriate method for handling sig figs in scientific data processing? If not, please provide guidance. Check the scripts\process_quantaq_data.py rolling average to ensure it is in minutes; change the current rolling average to a 10-minute rolling average; update and add any docstrings needed related to this or other missing items. Check the python_script_style.txt file for script styling and formatting. At any time, ask any questions that will help to complete the tasks.