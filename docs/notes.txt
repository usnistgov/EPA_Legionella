I need to make changes to how the data is processed and plotted:

1) The event_registry.csv and the event_log.csv file largely contain the same information. It would be helpful if this was a single well-organized file instead. 

2) The way the automated shower system works, there will never be a time when there is a co2 event logged and a shower event not logged. So, there is no need to make synthetic shower events. All code related to making synthetic shower event and saving that information to the log can be removed. 

3) For all event numbered events (even ones excluded from analyses), I would like to have all 5 event plots made. Right now, only the rh, temp, and wind figures are made. For the co2 and pm figures they would only be the measured data as the are not analyzed events. For all five of these plots for an excluded event I would like them save in an excluded_events subdirectory of the event_figures directory where other event figures are saved. 

4) In the co2_lambda_summary.csv, why is there a lambda_outside_r_squared and a lambda_entry_r_squared? Shouldn’t the R2 only be there for the co2 decay fit? Keep only the R2 for the decay fit, remove the others.

5) I would like to add a programmatic way to exclude events from the PM analysis. This will be a two-stage check. If the co2 derived lambda’s R2 is less than 0.9 then do not analysis the PM data, note in the single new log file (task 1 above) that the event is_excluded = TRUE (like other excluded events) and for the exclusion_reason Lambda R2  less then 0.9. The second check will be if the QuantAQ bedroom RH peak value occurs outside of the first 30 minutes of the deposition window, then bedroom is deemed not well mixed so like the co2 derived lambda’s R2 check, 
