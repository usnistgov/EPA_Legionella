I need to make changes to how the data is processed and plotted:

1) The event_registry.csv and the event_log.csv files largely contain the same information. It would be helpful if this were a single well-organized file instead. 

2) The way the automated shower system works, there will never be a time when there is a CO2 event logged and a shower event not logged. So, there is no need to make synthetic shower events. All code related to creating a synthetic shower event and logging that information can be removed. 

3) For all event-numbered events (even ones excluded from analyses), I would like to have all 5 event plots made. Right now, only the rh, temp, and wind figures are made. For the CO2 and pm figures, they would only be the measured data as they are not analyzed events. For all five plots for an excluded event, I would like them saved in an excluded_events subdirectory of the event_figures directory, alongside other event figures. 

4) In the co2_lambda_summary.csv, why is there a lambda_outside_r_squared and a lambda_entry_r_squared? Shouldn’t the R² only be there for the CO2 decay fit? Keep only the R2 for the decay fit; remove the others.

5) I would like to add a programmatic way to exclude events from the PM analysis. This will be a two-stage check. If the CO2-derived lambda’s R2 is less than 0.9, then do not analyze the PM data, note in the single new log file (task 1 above) that the event is_excluded = TRUE (like other excluded events), and for the exclusion_reason, Lambda R2  less than 0.9. The second check will be if the QuantAQ bedroom RH peak value occurs outside of the first 30 minutes of the deposition window. The bedroom is deemed not well mixed, so like the CO2-derived lambda’s R2 check, in the single new log file (task 1 above), the event is_excluded = TRUE (like other excluded events), and for the exclusion_reason, Bedroom is deemed to be not well mixed by RH analysis. So, like task 3, all five figures will be saved in the excluded_events subdirectory  
