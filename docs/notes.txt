1.	Review the (  )script docstring and ensure it properly captures the script's content and follows the docs/python_script_styles.txt guidelines. It is okay if the main scripts more strictly conform to the guidelines, whereas utility scripts can conform to a less strict version.
2.	Change the event_figures output process so that each of the different types of figures is in their own directory (i.e., excluded_events, co2_deacy, pm_decay, rh_timeseries, temperature_timeseries, wind_timeseries).
3.	For the *_pm_decay.png plots, reduce the time before the shower on to 1 hour.
4.	Add shower on and off to co2 plots.
5.	Adjust the *_summery.png plots to be more readable (text not running on plot, spacing, etc.). change should still use the Style Constants values; maybe adjust figure sizes to make them work.
6.	Review the *_pre_post_boxplot.png plots, the W23, W40pm, W40pw, and W52pm have problems where there is just an orange line for each value. 
7.	for the *_pm_decay.png plots, change the x10^# y-axis to be 1e# (for all figures that have large values (greater than 100) in the y-axis, change the axis to be 1e#). When beta decay is not valid, do not plot the instrument data as invalid; only the decay is invalid (still do not show calculated decay with not valid), not the data. 
8.	Most *_pm_decay.png plots look good, but something is up with event_69. There is way to much time after the end of the emissions to the end of figure (maybe has to do with the figure being set by the peak value time for the whole decay period vs peak decay within 30 mins.) 
9.	For the event_77-0220_w14_open_night_pm_decay.png figure, add the outdoor PM concentration for the same bins. Maybe make the outdoor concentration addition a utility script: if a variable is set (with event #s) at the start of the script, it will add the outdoor concentrations to those figures. 
10.	Add to the scripts\co2_decay_analysis.py script an argument that, if run with the script, will change how the data is processed. Without the argument, the script runs as it is. With the argument, instead of the end point for the decay being as it is, it will stop the fit when the entry concentration exceeds the bedroom.
11.	Change the particle size bins being analyzed from the current 7 bins to be bin0-11.
PARTICLE_BINS = {
0: {"name": "0.35-0.46", "min": 0.35, "max": 0.46, "column": "opc_bin0"},
1: {"name": "0.46-0.66", "min": 0.46, "max": 0.66, "column": "opc_bin1"},
2: {"name": "0.66-1.0", "min": 0.66, "max": 1.0, "column": "opc_bin2"},
3: {"name": "1.0-1.3", "min": 1.0, "max": 1.3, "column": "opc_bin3"},
4: {"name": "1.3-1.7", "min": 1.3, "max": 1.7, "column": "opc_bin4"},
5: {"name": "1.7-2.3", "min": 1.7, "max": 2.3, "column": "opc_bin5"},
6: {"name": "2.3-3.0", "min": 2.3, "max": 3.0, "column": "opc_bin6"},
7: {"name": "3.0-4.0", "min": 3.0, "max": 4.0, "column": "opc_bin7"},
8: {"name": "4.0-5.2", "min": 4.0, "max": 5.2, "column": "opc_bin8"},
9: {"name": "5.2-6.5", "min": 5.2, "max": 6.5, "column": "opc_bin9"},
10: {"name": "6.5-8.0", "min": 6.5, "max": 8.0, "column": "opc_bin10"},
11: {"name": "8.0-10.0", "min": 8.0, "max": 10.0, "column": "opc_bin11"},
12.	Write a new script that produces a time series Excel file for event 101 that is the data point for the predicted emission and the predicted decay for all the analyzed size bins. This is a one-off task to provide the data to a researcher. 
